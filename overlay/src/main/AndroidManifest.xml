<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:hasCode="false"
        android:label="TS18 Status Bar Geometry" />

    <!-- Android 10 RRO: same target as the supplied OEM sysbar geometry overlay.
         Priority 10 is intentionally higher than the supplied active OEM sysbar RRO
         priority 5 so this narrow override can take precedence without replacing it.
         The stock OEM overlay is not removed or replaced. -->
    <overlay
        android:isStatic="true"
        android:priority="10"
        android:targetPackage="android" />
</manifest>
